import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    height: 90,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 10,
    marginBottom: 10
  },
  icon: {
    width: 50,
    height: 50,
    borderRadius: 25,
    marginBottom: 10
  }
})

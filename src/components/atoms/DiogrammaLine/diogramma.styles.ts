import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 10,
    borderRadius: 2,
    flexDirection: 'row',
    marginBottom: 15
  },
  item: {
    height: '100%',
  }
})

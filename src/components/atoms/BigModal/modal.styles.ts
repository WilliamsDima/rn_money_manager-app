import { StyleSheet } from 'react-native'
import { COLORS } from '../../../services/colors'

export const styles = StyleSheet.create({
  modalStyle: {
    height: '100%',
    padding: 10,
    backgroundColor: COLORS.colorBgModal,
    position: 'relative'
  },
})

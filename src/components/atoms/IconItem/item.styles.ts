import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    height: 90,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 10,
    marginBottom: 10
  },
  icon: {
    width: 70,
    height: 70,
    borderRadius: 35,
    marginBottom: 10
  }
})

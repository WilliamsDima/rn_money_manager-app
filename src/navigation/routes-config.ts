import { COLORS } from "../services/colors"

export const screenOptions = {
  headerStyle: {
    borderBottomWidth: 0,
    // android
    elevation: 0,
    // ios
    shadowOpacity: 0,
    backgroundColor:  COLORS.colorHeader,
  },
}
